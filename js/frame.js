/* console.log('loading frame.js!');

function addJobSelectorEventListener() {
  console.log('doing addJobSelectorEventListener');
  // let jobSelectorElement = document.getElementById("jobSelector");
  let jobSelectorElement = document.getElementById("profile-experience");
  test();
  if (jobSelectorElement) {
    console.log('adding eventlistener');
    jobSelectorElement.addEventListener("change", changeJob);
    clearInterval(jobSelectorInterval);
  }
}

let jobSelectorInterval = setInterval(addJobSelectorEventListener, 1000);
*/
// Try until element exists
/* document.addEventListener("DOMContentLoaded", function () {
  console.log("DOM content loaded!");
  document.getElementById("jobSelector").addEventListener("change", changeJob);
});
*/
